const AdminForm = () => {
  return (
    <div>
      <h5>Admin Form Add Food Item</h5>
      <p>
        In Admin portion we have “Add food item” button. This button allows
        admins to open a Form component and add new food items. To create a form
        component, we will be importing some components from material UI.
      </p>
    </div>
  );
};
export default AdminForm;
